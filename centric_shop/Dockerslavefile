pipeline {
    agent {
        dockerfile {
            dir 'centric_shop'
         }
    }
    stages {
        stage('Test') {
             steps {
                dir('centric_shop') {
                   sh 'bundle install'
                   sh 'su slave'
                }
            }
        }
    }
}